services:
  - type: web
    name: fluxo-backend
    env: docker
    plan: free
    # Indica a Render que el contexto de construcción es la carpeta backend
    rootDir: .
    dockerContext: backend
    dockerfilePath: backend/Dockerfile
